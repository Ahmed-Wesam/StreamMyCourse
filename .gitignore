# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.*.local

# AWS
.aws/
*.pem

# IDE
.vscode/
.idea/
.cursor/plans/*
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
debug-*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Testing
.coverage
.nyc_output/
tests/unit/results.xml

# actionlint: local binary from download-actionlint.bash (commit skill / CI parity); optional global install avoids this
/actionlint
/actionlint.exe

# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# Lambda vendored dependencies (created by deploy-backend.sh pip install -t _vendor
# before zipping). Committed artifacts would bloat the repo and drift from
# requirements.txt.
infrastructure/lambda/catalog/_vendor/

# Misc
*.log
.cache/
temp/
tmp/

# Local scratch (not for the repo)
First_prompt.txt

# Operator-only notes (e.g. Cognito bootstrap scratch — not backed up by git)
.local-operator-notes.txt

# Git history backup (before history rewrite)
.backups/
