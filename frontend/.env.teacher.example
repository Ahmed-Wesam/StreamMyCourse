# Copy to frontend/.env.teacher (gitignored). Vite mode `teacher` merges this over .env.
# TeacherUserPoolClientId from CloudFormation output StreamMyCourse-Auth-<env> (see scripts/print-auth-stack-outputs.ps1).
VITE_COGNITO_USER_POOL_CLIENT_ID=your-teacher-app-client-id
